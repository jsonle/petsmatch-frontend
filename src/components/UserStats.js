import React from 'react';
import PetsList from './PetsList'

const UserStats = (props) => {
    return ( 
        <div>
            {/* <h1>{this.props.displayedUser.name}</h1> */}
        </div>
     );
}
 
export default UserStats;